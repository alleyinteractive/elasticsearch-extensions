<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="Elasticsearch Extensions" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">
  <description>PHP_CodeSniffer standard for Elasticsearch Extensions.</description>

  <!-- Include Alley Rules -->
  <rule ref="Alley-Interactive">
    <!-- Turn off spacing after param type because it does some undesirable things with phpstan annotations. -->
    <exclude name="Squiz.Commenting.FunctionComment.SpacingAfterParamType" />
  </rule>

  <!--
  Pass some flags to PHPCS:
    p flag: Show progress of the run.
    s flag: Show sniff codes in all reports.
  -->
  <arg value="ps"/>

  <!-- Enable colors in report -->
	<arg name="colors" />

  <!-- Whenever possible, cache the scan results and re-use those for unchanged files on the next scan. -->
  <arg name="cache" value=".phpcs/cache.json" />

  <!-- Strip the filepaths down to the relevant bit. -->
  <arg name="basepath" value="./"/>

  <!-- Check up to 20 files simultaneously. -->
  <arg name="parallel" value="20"/>

  <!-- Set severity to 1 to see everything that isn't effectively turned off. -->
  <arg name="severity" value="1"/>

  <!-- Exclude a few directories and autogenerated files. -->
  <exclude-pattern>vendor/</exclude-pattern>
  <exclude-pattern>tests/</exclude-pattern>

  <!-- The version set here matches the minimum version tested in the CI. -->
  <config name="minimum_supported_wp_version" value="6.5"/>

  <!-- Set the text domain for i18n. -->
  <rule ref="WordPress.WP.I18n">
    <properties>
      <property name="text_domain" type="array">
        <element value="elasticsearch-extensions" />
      </property>
    </properties>
  </rule>

  <!-- Set the prefixes for functions etc. -->
  <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
    <properties>
      <property name="prefixes" type="array">
        <element value="elasticsearch_extensions" />
      </property>
    </properties>
  </rule>

  <!-- Disallow functions deprecated and/or aliased for backwards compatibility -->
  <rule ref="Generic.PHP.DeprecatedFunctions"/>
  <rule ref="Generic.PHP.ForbiddenFunctions"/>
</ruleset>
